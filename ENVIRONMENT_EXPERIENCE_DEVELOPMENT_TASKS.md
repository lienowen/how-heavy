# 环境体验系统开发任务

项目：The Weight We Carry  
目标版本：H5 RC2  
优先目标：第一关环境体验达到可展示、可试玩、可量化验收的商业标准  
关联文档：`LEVEL_01_WEIGHT_SWAP_FUN_DESIGN.md`

## 一、开发原则

环境不能只是背景图片。环境必须持续读取玩家重量、速度、落地强度、所在区域和机关状态，并用运动、声音、光线、材质与路线变化回应玩家。

每个环境效果至少服务以下一个目的：

- 告诉玩家当前重量状态；
- 提示可用路线或危险；
- 确认一次操作已经生效；
- 展示重量对世界产生的物理影响；
- 强化关卡情绪和章节差异。

不服务玩法判断的效果不得遮挡平台、角色和交互目标。

## 二、里程碑安排

### ENV-M1：第一关环境闭环

完成重量状态、交换反馈、横风、踏板、闸门、裂纹地板及第一关音画联动。

### ENV-M2：四章节环境语言

把第一关验证后的系统扩展到 24 关，并为四个章节建立不同的环境主题。

### ENV-M3：H5 性能与可访问性

完成浏览器性能降级、Reduced Flashes、关闭震动、移动端适配和长时间稳定性测试。

## 三、P0 开发任务：第一关必须完成

### ENV-001 重量环境状态控制器

目标：建立唯一的环境状态入口，避免每个特效自行读取玩家。

开发内容：

- 新增 `EnvironmentResponseController`。
- 输入：玩家重量、水平速度、垂直速度、落地强度、当前区域、目标容器、机关状态。
- 输出：Light、Balanced、Heavy 三种环境状态及 0–1 的状态强度。
- 重量变化采用 0.25–0.4 秒插值，避免画面突然切换。
- 向风、尘埃、灯光、吊链、音频、镜头和 UI 发出统一事件。

验收：

- 玩家重量改变后 0.3 秒内所有环境系统收到相同状态。
- 连续快速交换不会残留上一状态。
- 暂停时环境动画和音频同步暂停。

依赖：无。  
预估：0.5 天。

### ENV-002 Light 状态环境反馈

目标：玩家不看数字也能感觉自己变轻。

开发内容：

- 增加横向风线、上浮尘粒和衣摆/悬挂物快速摆动。
- 跳跃时生成短暂轨迹弧线。
- 脚步和落地音降低低频，增加轻质空气音。
- 镜头垂直跟随略微放宽，强化滞空。
- 横风区域对 Light 的位移效果明显高于 Balanced 和 Heavy。

验收：

- 关闭 HUD 后，测试者能以至少 80% 正确率识别 Light。
- 风线不遮挡平台边缘和角色碰撞位置。
- Reduced Flashes 开启后仍能通过运动方向判断状态。

依赖：ENV-001。  
预估：1 天。

### ENV-003 Heavy 状态环境反馈

目标：让玩家感到重量进入地面和建筑结构。

开发内容：

- Heavy 移动时增加低频脚步、细尘和轻微地面震动。
- Heavy 落地时附近链条、灯具、秤臂产生衰减摆动。
- 画面顶部增加低透明压力线，底部边缘略微加深。
- 高速落地触发冲击环、尘埃和短镜头震动。
- 附近可破坏结构在重击前产生裂纹高亮。

验收：

- 关闭 HUD 后，测试者能以至少 80% 正确率识别 Heavy。
- 普通走路不频繁大幅震屏。
- 单次重击震动不超过 0.35 秒。
- 设置中关闭 Screen Shake 后完全无镜头位移。

依赖：ENV-001。  
预估：1 天。

### ENV-004 Balanced 状态与过渡

目标：让重量 6 成为清晰的中性基准，而不是“没有效果”。

开发内容：

- 风线、压力线和颜色偏移逐渐回归中性。
- 保留少量环境呼吸和机械运动，避免画面静止。
- 从 Light/Heavy 切回 Balanced 时播放短促稳定音。
- 平衡状态下秤盘和吊链回到中心位置。

验收：

- 三种状态转换没有突然闪烁。
- Balanced 仍有环境生命感，但不会盖过两端状态。

依赖：ENV-001、ENV-002、ENV-003。  
预估：0.5 天。

### ENV-005 交换目标空间提示

目标：玩家明确知道“自己”和“目标”分别是多少。

开发内容：

- 有效目标显示呼吸环、动态虚线和数字方向提示。
- 文案统一为 `CURRENT X → TARGET Y`。
- 相同重量不进入有效目标状态。
- 同重量靠近时显示 `SAME WEIGHT — FIND A DIFFERENT LOAD`。
- 目标离开范围后 0.15 秒内清除连线和高亮。
- 多目标时只高亮距离最近的不同重量容器。

验收：

- 不能出现角色是 6、目标也是 6，却仍提示可交换的情况。
- 目标数字始终与实际容器重量一致。
- 交换后玩家和容器数字在同一帧更新。

依赖：ENV-001。  
预估：0.5 天。

### ENV-006 交换瞬间环境冲击

目标：让交换成为游戏的核心表演动作。

开发内容：

- 交换开始：玩家与容器之间产生能量流。
- 交换中：两个数字沿连线互换位置。
- 交换完成：环境在 0.3 秒内切换到新重量状态。
- Light 交换完成使用高频空气声；Heavy 使用低频机械锁定声。
- 加入 0.18 秒输入锁，避免一次按键重复触发。
- 仅实际重量变化时增加交换统计和触发成就。

验收：

- 交换响应延迟小于 100 毫秒。
- 交换前后资源守恒。
- 长按 E 不会每帧反复交换。
- 100 次快速输入不会卡死或产生重复连接。

依赖：ENV-001、ENV-005。  
预估：1 天。

### ENV-007 教学横风区

目标：用环境证明 Light 既有能力也有风险。

开发内容：

- 第一关跨越缺口后增加短横风区。
- 使用粒子、布条、尘雾和声场明确表现风向。
- Light 明显偏移，Balanced 可控制，Heavy 基本稳定。
- 设置安全下层回路，失败后 0.6 秒内重新尝试。
- 风力进入和退出采用渐变，禁止无提示瞬间加速。

验收：

- 三种重量在相同输入下产生可量化的不同位移。
- 风向无需文字即可判断。
- Light 失败不会造成软锁或长时间等待。

依赖：ENV-002。  
预估：1 天。

### ENV-008 重型踏板、秤臂与闸门联动

目标：让建筑表现出“承受重量”。

开发内容：

- 踏板上直接显示阈值 10。
- 重量不足时踏板轻微下沉后回弹，并播放拒绝声。
- Weight 10 站上后，踏板下沉、秤臂倾斜、链条绷紧、闸门开启。
- 机关动作按因果顺序传播，而不是所有物件同帧切换。
- 闸门开启后保持稳定，避免玩家离开踏板立即被夹住。

推荐时序：

1. 0–0.12 秒：踏板下降；
2. 0.08–0.28 秒：链条绷紧；
3. 0.18–0.55 秒：秤臂倾斜；
4. 0.35–0.9 秒：闸门开启；
5. 0.9 秒：完成音和出口光出现。

验收：

- 2 和 6 无法开启阈值 10 机关。
- 10 连续 20 次均能可靠开启。
- 动画过程碰撞体与画面一致。
- 闸门不能夹死角色。

依赖：ENV-003。  
预估：1.5 天。

### ENV-009 裂纹地板破坏反馈

目标：让 Heavy 的破坏能力形成第一关高潮。

开发内容：

- 地板至少有完整、预裂、破碎三个视觉状态。
- Light/Balanced 落地只触发短暂裂纹提示。
- Heavy 低速站立不破坏；达到冲击速度阈值才破碎。
- 破碎产生碎片、尘埃、低频冲击和安全落点提示。
- 碎片不创建大量永久物理对象，避免 H5 性能下降。

验收：

- 重量和速度两个条件同时参与判定。
- 2、6 连续测试均不能误触发破坏。
- 10 连续 20 次重击均能触发。
- 破坏后不存在不可见碰撞体。

依赖：ENV-003。  
预估：1 天。

### ENV-010 第一关空间美术重排

目标：解决角色、重量容器和环境道具重叠的问题。

开发内容：

- 出生点与首个容器保持至少 90 像素可读间距。
- 背景高对比区域不能位于角色和平台边缘正后方。
- 交互容器使用统一轮廓和地面底座。
- 平台、危险、可破坏物和装饰物建立明确视觉层级。
- HUD 顶部黑边和游戏画面比例重新检查，减少被压缩感。

验收：

- 1280×720、1920×1080 下角色和容器均不重叠。
- 玩家能在 3 秒内指出角色重量和最近容器重量。
- 黑边、HUD 和玩法空间没有异常裁切。

依赖：ENV-005。  
预估：1 天。

## 四、P1 开发任务：提升商业质感

### ENV-011 动态吊链与灯具

- 使用轻量级程序动画，不使用大量刚体。
- 响应风、Heavy 落地和机关动作。
- 摄像机外停止更新。

### ENV-012 局部雾与景深层次

- 建立前景、中景、远景三层雾。
- 玩家附近保持足够对比度。
- 风区改变雾流方向，Heavy 冲击短暂排开地面雾。

### ENV-013 区域声场

- 风区、机关区、裂纹区拥有独立循环环境音。
- 根据摄像机距离平滑混音。
- 暂停、失焦和广告时正确静音或停止。

### ENV-014 材质状态变化

- 踏板受力后出现压痕和金属摩擦亮边。
- 裂纹地板逐级加深。
- 闸门运动时链条和轨道产生局部高光。

### ENV-015 环境叙事标记

- 用旧秤、损坏记录、遗留容器表现世界曾经发生过什么。
- 不用大段文字解释剧情。
- 每个章节至少有一个可辨认的环境叙事母题。

## 五、四章节环境主题任务

### ENV-CH1 Measure

- 主题：测量、刻度、秤、秩序。
- 环境响应：仪表、刻度和秤臂直接读取重量。
- 色彩：冷黑、黄铜、青色能量。

### ENV-CH2 Balance

- 主题：平衡、互相依赖、可逆选择。
- 环境响应：双平台、配重桥、同步秤盘。
- 色彩：灰绿、旧金、柔和雾层。

### ENV-CH3 Momentum

- 主题：速度、惯性、风和移动结构。
- 环境响应：强风、移动平台、旋转机构。
- 色彩：深蓝、青白、冷紫。

### ENV-CH4 Cost

- 主题：代价、破坏、不可逆选择。
- 环境响应：裂纹、坠落、过载、结构崩塌。
- 色彩：暗红、焦铜、灰烬。

## 六、P0 自动化测试任务

### ENV-QA-001 状态同步测试

- 2、6、10 切换后检查所有环境子系统状态一致。
- 连续切换 100 次无残留状态。

### ENV-QA-002 交换输入测试

- 同重量无效交换被阻止。
- 不同重量正常交换。
- 长按、连按、键盘、手柄和触控结果一致。

### ENV-QA-003 风力差异测试

- 固定时间、固定输入下记录三种重量位移。
- 位移必须符合 Light > Balanced > Heavy。

### ENV-QA-004 机关阈值测试

- 测试阈值边界、进入、离开、暂停、恢复和角色死亡。

### ENV-QA-005 裂纹地板测试

- 覆盖三种重量和多个落地速度。
- 检查画面状态与碰撞状态一致。

### ENV-QA-006 性能压力测试

- 同屏启用全部第一关环境效果并运行 10 分钟。
- 监测对象数量、帧时间和内存增长。
- 确保离屏效果停止更新。

## 七、H5 性能预算

- 目标：桌面浏览器稳定 60 FPS。
- 移动设备最低目标：稳定 30 FPS。
- 环境程序粒子建议同时可见不超过 120 个。
- 装饰链条使用绘制或骨骼动画，不批量使用物理刚体。
- 碎片效果生命周期不超过 1.5 秒。
- 环境控制器每帧不得遍历整个场景树。
- 离开摄像机范围的动态装饰停止 `_process`。
- Reduced Effects 模式将粒子、雾和装饰运动减少至少 50%。

## 八、开发执行顺序

1. ENV-001 重量环境状态控制器。
2. ENV-005 交换目标空间提示。
3. ENV-006 交换瞬间环境冲击。
4. ENV-002、ENV-003、ENV-004 三种重量状态。
5. ENV-010 第一关空间重排。
6. ENV-007 横风区。
7. ENV-008 重型踏板与闸门。
8. ENV-009 裂纹地板。
9. ENV-QA-001 至 ENV-QA-006。
10. ENV-011 至 ENV-015 商业质感增强。
11. 扩展到 ENV-CH1 至 ENV-CH4。
12. ENV-M3 H5 性能与可访问性收口。

## 九、第一关环境体验完成标准

必须同时满足：

- 玩家不看 HUD 也能大致判断自己是 Light、Balanced 还是 Heavy。
- 每次有效交换在 0.3 秒内产生环境变化。
- 第一关至少有一次环境帮助 Light、一次环境惩罚 Light、一次环境需要 Heavy。
- 玩家能看到重量进入踏板、秤臂、链条、闸门和地板的因果过程。
- 不存在同重量无效交换和角色/容器视觉重叠。
- 自动化测试全部通过。
- 未阅读说明的玩家对“重量改变环境”的体验评分达到 4/5。

达到以上标准后，第一关才可进入商业版试玩验收。
